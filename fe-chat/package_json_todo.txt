{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "lint:fix": "next lint --fix",
    "type-check": "tsc --noEmit",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build:analyze": "ANALYZE=true next build",
    "prepare": "husky install"
  }
}

---

per le dipendenze 

{
  "prettier": "^3.0.0",
  "husky": "^8.0.0",
  "lint-staged": "^15.0.0",
  "@next/bundle-analyzer": "^15.0.0",
  "jest": "^29.0.0",
  "jest-environment-jsdom": "^29.0.0",
  "@testing-library/react": "^14.0.0",
  "@testing-library/jest-dom": "^6.0.0"
}


--
// Dipendenze aggiuntive consigliate per chat
{
  "dependencies": {
    // Gestione stato chat
    "zustand": "^4.0.0", // State management leggero
    "react-query": "^3.0.0", // Cache messaggi
    
    // UI chat-specific
    "react-textarea-autosize": "^8.0.0", // Textarea espandibile
    "react-window": "^1.8.0", // Virtualizzazione messaggi
    
    // Utilities
    "date-fns": "^2.0.0", // Gestione date messaggi
    "emoji-picker-react": "^4.0.0", // Emoji picker
    
    // Validazione
    "zod": "^3.0.0" // Schema validation messaggi
  }
}


Raccomandazioni Prioritarie 🎯
1. Immediata (Essenziale)

Aggiungi Prettier per formatting consistente
Configura Husky + lint-staged per quality gates
Crea jest.config.js per testing

2. Breve termine (Importante)

Implementa bundle analyzer per ottimizzazione
Aggiungi testing utilities per componenti chat
Configura environment variables per diversi ambienti

3. Lungo termine (Ottimizzazione)

Implementa PWA features per app chat mobile
Aggiungi monitoring tools (Sentry, LogRocket)
Configura CI/CD pipeline con GitHub Actions